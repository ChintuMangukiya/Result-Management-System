export interface ICallRecord {
    name: string;
    callId: number;
    duration: number;
    switchCode: string;
    direction: string;
    number: string;
    id: string
}

export interface marksArray {
    marks:number,
    subject: string
}